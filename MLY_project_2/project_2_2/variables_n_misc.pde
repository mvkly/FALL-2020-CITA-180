
// === GENERAL GLOBAL VARIABLES ===
Role role;
Intro intro; 
//String[] challengeArray = new String[10]; 
//CrewTransit CrewTransit;
//CrewTransit[] transit = new CrewTransit[10]; 
int clicks = 0;

// mouse tail
int num = 50;
int[] x = new int[num];
int[] y = new int[num];


//Intro intro;
  float[] xstar = new float[100];
  float[] ystar = new float[100];
  float[] starspeed = new float[100];
  
 

// === MISC ===
    // transit.displayCrew();
 //if (mousePressed == true && mouseButton == LEFT) {
 //  //  && mouseButton == LEFT
 // clicks += 1;
  //challenge.crewmem();
 //}
 
 //if (mousePressed == true && mouseButton == RIGHT) {
 //clicks -= 1;
 //}
 
 // crewmate
 //if (mousePressed == true && mouseX < width/2 && mouseButton == LEFT) {
 // clicks += 1;
 // // transition to crewmate
 //}
 
 // imposter
 //if (mousePressed == true && mouseX > width/2 && mouseButton == LEFT) {
 //  clicks += 1;
 //}
 
 //else {
 //  clicks += 0; }
 
  //println("Amount of clicks:", clicks);  // clicks seem to accumulate based on length of click
  
  
  //void mouseClicked() {
// clicks += 1; 
//}
